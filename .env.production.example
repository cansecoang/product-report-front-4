# Production Environment Variables
# Copy this file to your Azure App Service Application Settings

# Database Configuration (Required)
# Option A: Use DATABASE_URL (Recommended for single connection string)
DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# Option B: Use individual variables (Alternative to DATABASE_URL)
DB_HOST=your-postgresql-host.postgres.database.azure.com
DB_PORT=5432
DB_NAME=your_database_name
DB_USER=your_username
DB_PASSWORD=your_secure_password

# Azure App Service Configuration
NODE_ENV=production
PORT=80
WEBSITE_NODE_DEFAULT_VERSION=22-lts

# Optional: For enhanced logging and debugging
DEBUG=false
LOG_LEVEL=info

# Security Settings (automatically applied by Next.js config)
# No additional configuration needed - handled by next.config.ts headers

# Example for Azure PostgreSQL Database
# Replace with your actual Azure PostgreSQL connection details:
# DATABASE_URL=postgresql://biofincas@biofincas-server:your_password@biofincas-server.postgres.database.azure.com:5432/biofincas_db?sslmode=require